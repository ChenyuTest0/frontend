{
  "version": "0.0.1",
  "configurations": [
    {
      "type": "pwa-chrome",
      "request": "launch",
      "name": "Debug from VSCode",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/src"
    }
  ]
}
